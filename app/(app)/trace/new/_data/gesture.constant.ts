/** 사진을 화면에 맞춘 상태. 이보다 작게는 줄지 않는다 — 빈 곳이 드러난다. */
export const MIN_ZOOM_SCALE = 1

/** 어절 하나가 손가락으로 집을 만해지는 배율. 더 키워도 화질이 따라오지 못한다. */
export const MAX_ZOOM_SCALE = 4

/**
 * 어절 상자 바깥으로 두는 탭 여유(화면 px).
 *
 * 어절은 손가락 끝보다 작아서 정확히 겹쳐야만 잡히면 자꾸 빗나간다. 옆 어절까지
 * 딸려오지 않을 만큼만 넓힌다. 확대 배율로 나눠 써서 화면에서 보이는 여유는 늘 같다.
 */
export const TAP_TOLERANCE = 6

/**
 * 여기까지 움직인 제스처는 탭으로 본다(화면 px). 손가락은 가만히 있어도 몇 px 떨린다.
 *
 * 탭은 어절 하나를 뒤집고, 그 이상 움직이면 지나간 어절을 훑는 드래그다.
 * 이 둘을 가르지 않으면 어절 하나를 빼려던 탭이 떨림 탓에 옆줄까지 함께 잡는다.
 */
export const TAP_SLOP = 8
