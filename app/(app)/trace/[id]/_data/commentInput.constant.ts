/**
 * 서버 스펙(댓글 content maxLength)과 같은 값 — 새 댓글 입력바(CommentBar)와 인라인 수정
 * 입력(CommentEditForm)이 같은 상한을 쓴다.
 * 두 자리에 따로 적어두면 한쪽만 고쳐져 "쓸 때는 되는데 고칠 때는 잘리는" 상태가 된다.
 */
export const COMMENT_MAX_LENGTH = 500
