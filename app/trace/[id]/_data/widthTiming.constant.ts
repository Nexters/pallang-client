/* 종이 폭만 따로 타이밍을 주는 진행률 식(CSS 값).
   기울기·상승·높이·배너·탭은 재조판과 무관해 전부 --collapse를 그대로 따르고,
   유일하게 줄바꿈을 다시 계산하게 만드는 폭 변화만 이 식을 따른다.

   전환이 끝나는 순간에만 계단식으로 바뀌므로 재조판은 1회, 전환 중 줄바꿈 셔틀림은 0이다. */
export const WIDTH_TIMING = 'clamp(0, calc((var(--collapse) - 0.999) * 1000), 1)'
